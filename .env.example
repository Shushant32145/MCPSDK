# Pine SDK MCP Server configuration
# Copy this to `.env` and adjust values as needed.

# Network
MCP_HOST=0.0.0.0
MCP_PORT=8080

# Transport: stdio | sse | streamable-http
MCP_TRANSPORT=streamable-http

# Logging level: DEBUG | INFO | WARNING | ERROR | CRITICAL
LOG_LEVEL=INFO
